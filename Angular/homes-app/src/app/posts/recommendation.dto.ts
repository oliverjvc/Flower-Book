// src/app/recommendation.dto.ts

export interface RecommendationDTO {
    recommendationText: string; // Adjust this based on your actual fields
    userId: number; // Assuming userId is a number
  }
  